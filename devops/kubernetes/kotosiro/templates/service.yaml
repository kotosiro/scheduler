apiVersion: v1
kind: Service
metadata:
  name: {{ include "kotosiro.fullname" . }}
  labels:
    {{- include "kotosiro.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kotosiro.selectorLabels" . | nindent 4 }}
